# GFE Addons

Put each addon in its own folder:

```text
addons/
  my-addon/
    addon.json
    addon.py
```

GFE scans this folder at startup. Addons are enabled by default when first discovered and can be checked or unchecked in the GFE addon panel.
